name: masmorra_ascii
version: 0.33.0
description: Masmorra ASCII - Step 33 - Strategy e Command para IA
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
