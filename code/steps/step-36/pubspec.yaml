name: masmorra_ascii
version: 0.35.0
description: Masmorra ASCII - Step 35 - Máquinas de Estado para IA Inteligente
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
