name: masmorra_ascii
version: 0.29.1
description: Masmorra ASCII - Step 30 - Async, Await e o Tempo na Masmorra
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:

dev_dependencies:
  test: ^1.24.0
  lints: ^5.0.0
